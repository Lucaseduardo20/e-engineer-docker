# SPRINT 1 - PLANO EXECUTIVO (CÓDIGO E AÇÕES)

**Objetivo:** Implementar as tarefas da Sprint 1 conforme o ROADMAP-GLOBAL.md
**Timeline:** 72-96h (33-40 horas)

---

## Tasks resumidas (com passos concretos)

### MVP-001: Refinar UI de projetos (Vuetify integration) — 6h
- Verificar `main.ts` e garantir Vuetify plugin instalado
- Converter `ProjectsList.vue` para usar `v-data-table` ou `v-list`
- Usar `v-card`, `v-btn`, `v-text-field` para ações padrão
- Testar visualmente e ajustar tokens de CSS

Exemplo rápido de componente (snippet):
```vue
<template>
  <v-data-table :items="projects" :loading="loading">
    <template #item.name="{ item }">
      <v-btn text @click="open(item.id)">{{ item.name }}</v-btn>
    </template>
  </v-data-table>
</template>
```

Validação:
- `npm run type-check`
- Visual check: `/projects` possui estilo Vuetify consistente

---

### MVP-002: Add filter/search em projects list — 3h
- Frontend: adicionar `v-text-field` para search e `v-select` para status
- Backend: garantir `GET /projects` aceita `?name=&status=&page=&limit=` e aplica filtros
- Testes: unit test para query builder do repository

Exemplo query params:
```
GET /projects?name=ponte&status=active&page=1&limit=10
```

Validação:
- UI filtra corretamente
- Network shows filtered requests

---

### MVP-003: Validar error handling completo — 3h
- Backend: padronizar exceptions (HttpException com payload `{ code, message, details? }`)
- Frontend: interceptor para exibir mensagens amigáveis (toast/snackbar)

Validação:
- Simular 400/401/500 e checar menssagens no UI

---

### MVP-004: Refresh token flow — 4h
- Backend: revisar `refresh` logic e expiração; garantir revoke/rotation se aplicável
- Frontend: confirmar interceptor retry já implementado na Sprint 0
- Teste: simular expiração e garantir renovação sem logout

Validação:
- Expire token artificially and ensure requests refresh token and retry

---

### MVP-005: Add validação global (class-validator) — 2h
- Garantir DTOs com decorators (`@IsString()`, `@IsNotEmpty()`, etc.)
- GlobalValidationPipe no `main.ts` com `whitelist: true` e `forbidNonWhitelisted: true`

Snippet:
```typescript
app.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true }))
```

Validação:
- Testar POST /projects com campos inválidos → 400 com details

---

### MVP-006: Add loading states em todas as requisições — 3h
- Frontend: garantir stores expõem `loading` e componentes consomem
- Mostrar skeletons ou `v-progress-linear` no lugar correto

Validação:
- Manual check during slow network

---

### MVP-007: Tests - Backend 60% coverage — 6h
- Adicionar unit tests para use cases (`identity`, `projects`)
- Adicionar integration tests com in-memory DB ou testcontainers

Validação:
- `npm run test:cov` shows >= 60% for backend critical modules

---

### MVP-008: Tests - Frontend 40% coverage — 4h
- Vitest unit tests for `LoginForm.vue`, `ProjectsList.vue`, stores
- Snapshots where applicable

Validação:
- `npm run test:unit --coverage` shows >= 40% on frontend critical areas

---

### MVP-009: Docs - Guia QA para testar fluxo MVP — 2h
- Criar `sprints/sprint-1/QA-MVP-Sprint1.md` com passos manuais e checklist
- Incluir cenários de regressão

---

## Ordem de execução (prioridade)
1. MVP-005 (global validation) — low-risk, rápido
2. MVP-004 (refresh token) — garante estabilidade de sessão
3. MVP-006 (loading states) — UX melhoria contínua
4. MVP-001 (Vuetify refactor) — visual e consistência
5. MVP-002 (filters/search) — depende de backend
6. MVP-003 (error handling) — cross-cutting
7. MVP-007 / MVP-008 (tests) — paralelizáveis
8. MVP-009 (docs) — final

---

## Entregáveis finais
- Implementações no frontend/backend conforme tasks
- Testes cobrindo happy path
- QA guide em `/sprints/sprint-1/QA-MVP-Sprint1.md`

**Fim do plano executivo**
